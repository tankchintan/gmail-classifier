# OAuth credentials and tokens (NEVER commit these!)
credentials.json
credentials-*.json
token*.pickle

# Private classification rules (personal sender addresses — keep gitignored)
scripts/classification_rules.personal.json
scripts/classification_rules.work.json
# Legacy monolithic rules file (replaced by base+personal split)
scripts/classification_rules.json

# Profile configs (contain machine-specific paths)
profiles/personal.json
profiles/work.json

# Personal notes and temp files
linkedin-article-raw-notes.md
tmp/

# launchd plists (machine-specific paths, regenerate per machine)
com.ctank.*.plist

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so

# Data and logs (too large / sensitive, per-profile)
data/personal/
data/work/
logs/personal/
logs/work/
audit-reports/*.md

# Session notes and build contracts (personal, not part of project)
SESSION_SUMMARY.md
dashboard/CONTRACT.md

# But keep directories
!data/.gitkeep
!logs/.gitkeep
!audit-reports/.gitkeep

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
