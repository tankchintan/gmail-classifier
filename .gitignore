# OAuth credentials and tokens (NEVER commit these!)
credentials.json
token.pickle
token-modify.pickle

# Private classification rules (personal sender addresses — keep gitignored)
scripts/classification_rules.personal.json
# Legacy monolithic rules file (replaced by base+personal split)
scripts/classification_rules.json

# Personal notes
linkedin-article-raw-notes.md

# launchd plist (machine-specific paths, regenerate per machine)
com.ctank.gmail-classifier.plist

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so

# Data and logs (too large / sensitive)
data/*.csv
data/*.json
logs/*.jsonl
audit-reports/*.md

# But keep directories
!data/.gitkeep
!logs/.gitkeep
!audit-reports/.gitkeep

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
